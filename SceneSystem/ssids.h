
#pragma once
#include "MakeID.h"
#include "rsids.h"

namespace ss 
{
    MAKEID_TYPE(VolumeSliceID);
    MAKEID_TYPE(GridID);
    MAKEID_TYPE(TriadID);
    MAKEID_TYPE(BoundsID);
    MAKEID_TYPE(VolumeModelID);
    MAKEID_TYPE(AssetModelID);
}
